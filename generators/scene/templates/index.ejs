import React, { memo } from "react";
import { use<%= sceneName %> } from "./index.hooks";

type <%= sceneName %>Props = {}

export const <%= sceneName %> = memo(({}: <%= sceneName %>Props) => {
  const {} = use<%= sceneName %>();

  return (
    <div />
  );
});

<%= sceneName %>.displayName = "<%= sceneName %>";
