import React, { memo } from "react";
import { use<%= pageName %> } from "./index.hooks";
import <%= spaName %>Spa from "spas/<%= spaFolderName %>";

type <%= pageName %>Props = {}

const <%= pageName %> = memo(({}: <%= pageName %>Props) => {
  const {} = use<%= pageName %>();

  return <<%= spaName %>Spa />;
});

export default <%= pageName %>;
