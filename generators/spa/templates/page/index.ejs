import React, { memo } from "react";
import { use<%= pageName %> } from "./index.hooks";
import <%= spaName %>AppWrapper from "spas/<%= spaFolderName %>/AppWrapper";

type <%= pageName %>Props = {}

const <%= pageName %> = memo(({}: <%= pageName %>Props) => {
  const {} = use<%= pageName %>();

  return <<%= spaName %>AppWrapper />;
});

export default <%= pageName %>;
